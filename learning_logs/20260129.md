# 📂 01.29 수업 내용  
# 📍 안내사항
## 1-1
(가) - (kor + eng + math) / 3;

(나) - String pass = (avg ≥ 60) ? “합격” : “불합격”;

## 1-2
(가) System.out.println("이름: “ + name + ”, 나이: " + age);

(나) (age > 20) ? true : false;

## 1-3
(가) - (2)

(나) - total += price2 * qty2;

## 2-1
(가) - for (int i = 0; i < scores.length; i++) {
    sum += scores[i];
}

(나) - for (int i = 0; i < scores.length; i++) {
    if (scores[i] > 79) {
        System.out.print(scores[i] + " ");
    }
}

## 2-2
(가) - String[] orders = raw.split(",");

(나) - for(int i = 0; i < orders.length; i++) {
    orders[i] = orders[i].trim();
}

## 3-1
ui_test.zip 제출

## 3-2
1. 동일한 변수명으로 새롭게 선언(재선언)하여도 오류가 나지 않아서 원하는 결과값이 다르게 발생할 수 있다. 그리고 변수의 범위 또한 전역범위 또는 함수 범위로 지정되기때문에 문제가 발생할 수 있고, 호이스팅 현상이 일어난다. 그래서 이를 방지하고자 재선언이 불가능하고 블록 범위를 가진 let 키워드를 사용한다.

2. king 객체에 있는 key 이름을 그대로 사용해야한다.
const { birth, name, tomb, death } 에 있는 tomb를 tombName으로 수정해야한다.

3. map() 함수로 users의 id값들이 2배로 되었을 뿐, 반환되는 값이 없어서 userMap에는 아무것도 들어있지 않다.
그래서 retrun을 넣어야 하고, users 배열을 변경하지 않으면서 새로운 배열을 생성하려면
const userMap = users.map(user => {

    return user.id * 2;

});
로 작성해야한다.

# 🟦 (주제1)

## 🔷 1. (소주제)

### 🔹 1번

🛎️ 실습
```java
코드
```

# 🟦 (주제2)



# 🚨발생한 문제


# 📖 복습 & 확인
✔️ 내용
💡📌📍🚩🚨⚠️